# 项目实时状态（PROJECT_STATE）

> **这是项目的单一事实来源（驾驶舱）。任何 AI 接手，先看这一个文件。**
> 30 秒速览 → 看下面「当前状态」；要深入 → 按「延伸阅读」跳转。

**项目**：财务到款认领系统（飞书企业自建应用，单文件 FastAPI + SQLite）。
**跑起来**：`cp .env.example .env`（Secret 找 Lear）→ `pip install -r requirements.txt` → `uvicorn app:app --port 8000`。
**延伸阅读**：接手指引 `CODEX_ONBOARDING.md` → 决策与坑 `DECISIONS.md` → 架构 `ARCHITECTURE.md` → 约定 `DEVELOPMENT_RULES.md`。
**仓库**：`git@github.com:Leariceleto/claim-pool.git`（private）。负责人 **Lear**（超管 open_id `ou_9c5ee286ee29ba68b58b97b0dc3087d6`）。

---

## 维护协议（写给维护本项目的 AI——包括你）

**铁律：每完成一个开发任务、提交代码前，必须更新本文件，并随代码一起提交。** 这样本文件永远反映最新状态，新 AI 只读它就能接手。

每次更新做四件事：
1. 更新顶部「**最后更新**」的日期和提交说明。
2. 在「**最近完成**」顶部加一条（倒序，**只保留最近 6 条**，更早的删掉——历史决策去 `DECISIONS.md` 查，不在这里堆积）。
3. 调整「**当前待办**」：完成的划掉/移除，新出现的加上，按优先级排。
4. 更新「**已知问题**」：修掉的删除，新发现的补上。

写的时候保持简洁、只写"现在的事实"，不写过程叙述。其他 `.ai/*.md` 文档（架构、决策、规则）是相对稳定的参考，**只在对应内容真正变化时才改**；高频变动的状态只放本文件。

> 可选增强：可配 git `pre-commit` 钩子检查本次提交是否包含本文件的改动，没有则提醒。核心仍是 AI 自觉遵守上面的协议。

---

## 当前状态

**最后更新**：2026-06-24 ｜ 部分认领的到款继续留在待认领列表，状态显示为“部分认领中”。
**一句话**：功能已基本完整、可试用；支持 Excel / PDF 导入、部分金额认领、财务自助管理项目、已关闭单据 7 天后隐藏；认领搜索仅飞书登录后可见；**尚未正式部署**。

## 最近完成（倒序，最多 6 条）

1. **2026-06-24** 部分认领列表保留：`partial_claiming` 到款继续出现在待认领列表和日期筛选结果中，便于其他部门继续认领余额。
2. **2026-06-23** 财务项目管理：管理员可在现有部门/中心下新增或删除项目；删除仅停用后续选择，历史认领保留。
3. **2026-06-23** 已关闭单据自动隐藏：记录首次关闭时间，后台列表展示 7 天后隐藏，数据、审计和导出仍保留。
4. **2026-06-23** 银行 Excel 表头兼容：新增“交易日”和“收(付)方名称”映射，避免整表误按无表头固定列顺序解析。
5. **2026-06-22** Excel 导入与列表展示修正：新增“贷方金额”表头映射；后台“付款方 / 摘要”完整换行显示，不再混入附件链接。
6. **2026-06-22** Excel 文件导入：支持解析 `.xlsx` / `.xls` 的所有非空工作表，自动识别前 20 行内的表头并汇入待确认批次。

## 当前待办（按优先级）

1. **【最高】正式部署**：按 `DEPLOY.md` 部署到 Zeabur 或腾讯云轻量，替换临时隧道；配持久卷、环境变量，回填飞书回调地址并在飞书重新发布。
2. **部署后收尾**：飞书应用可见范围设为全员/目标同事；重置一次飞书 App Secret（曾明文出现过）；清理 `claim_pool.db` 里的开发期演示数据。
3. **转交 Codex** 继续维护。

## 已知问题

- 临时隧道（trycloudflare）地址会变、跨天失效，仅开发期可用（正式部署后消失）。
- 飞书部门未自动从组织架构读取，靠用户首次自选 + 管理员后台调整（原因见 `DECISIONS.md`，是有意取舍）。
- `claim_pool.db` 混有开发期演示数据（含与现 `catalog.json` 不一致的旧部门名历史记录）。
- 驳回通知依赖接收人在飞书应用可见范围内，范围外发送失败（容错不报错，但人收不到）。
- 单文件 `app.py`（~1980 行、HTML 内联）、无自动化测试，靠手动验证回归。
- SQLite 单文件，适合当前小规模（内部工具、几十人）；并发写多/数据量大需迁 PostgreSQL。数据库不在 git，需单独备份。
