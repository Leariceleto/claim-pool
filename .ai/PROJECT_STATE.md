# 项目实时状态（PROJECT_STATE）

> **这是项目的单一事实来源（驾驶舱）。任何 AI 接手，先看这一个文件。**
> 30 秒速览 → 看下面「当前状态」；要深入 → 按「延伸阅读」跳转。

**项目**：财务到款认领系统（飞书企业自建应用，单文件 FastAPI + SQLite）。
**跑起来**：`cp .env.example .env`（Secret 找 Lear）→ `pip install -r requirements.txt` → `uvicorn app:app --port 8000`。
**延伸阅读**：接手指引 `CODEX_ONBOARDING.md` → 决策与坑 `DECISIONS.md` → 架构 `ARCHITECTURE.md` → 约定 `DEVELOPMENT_RULES.md`。
**仓库**：`git@github.com:Leariceleto/claim-pool.git`（private）。负责人 **Lear**（超管 open_id `ou_9c5ee286ee29ba68b58b97b0dc3087d6`）。

---

## 维护协议（写给维护本项目的 AI——包括你）

**铁律：每完成一个开发任务、提交代码前，必须更新本文件，并随代码一起提交。** 这样本文件永远反映最新状态，新 AI 只读它就能接手。

每次更新做四件事：
1. 更新顶部「**最后更新**」的日期和提交说明。
2. 在「**最近完成**」顶部加一条（倒序，**只保留最近 6 条**，更早的删掉——历史决策去 `DECISIONS.md` 查，不在这里堆积）。
3. 调整「**当前待办**」：完成的划掉/移除，新出现的加上，按优先级排。
4. 更新「**已知问题**」：修掉的删除，新发现的补上。

写的时候保持简洁、只写"现在的事实"，不写过程叙述。其他 `.ai/*.md` 文档（架构、决策、规则）是相对稳定的参考，**只在对应内容真正变化时才改**；高频变动的状态只放本文件。

> 可选增强：可配 git `pre-commit` 钩子检查本次提交是否包含本文件的改动，没有则提醒。核心仍是 AI 自觉遵守上面的协议。

---

## 当前状态

**最后更新**：2026-08-24 ｜ 修正 POS 判重与待确认批次取消。
**一句话**：功能已基本完整、可试用；支持 Excel / PDF 导入、无流水号表格重复行保留、POS 流水按完整关键字段组合判重、到款公司记录、搜索页批量认领、分摊认领及退款负数、部分金额认领、所有认领提交后直接已认领、本人取消认领、本人取消认领后管理员私信通知、管理员驳回退回待认领、入池群通知、驳回私信通知、跨部门参与范围、全部角色个人看板、个人看板日期区间筛选和分摊认领信息展开查看、个人中心我的认领长列表渐进显示、财务自助新增部门、中心/小组和项目、按日期区间 CSV/纯文本导出、已关闭单据 7 天后隐藏；认领搜索仅飞书登录后可见；**尚未正式部署**。

## 最近完成（倒序，最多 6 条）

1. **2026-08-24** 导入与批次：有流水号时按日期、流水号、到款公司、金额、付款方和摘要组合判重，避免 POS 流水号复用时误跳过；待确认流水不能单独关闭，历史上被误关闭且无认领记录的待确认批次可正常取消。
2. **2026-08-21** 飞书通知：财务确认批次入池后的群消息不再展示金额和笔数，精简为入池完成及查看认领提示；后台统计和审计数据不变。
3. **2026-08-03** 个人看板：新增开始日期、结束日期筛选，支持跨月查看任意到款日期区间；筛选与身份范围切换互相保留；仅有一个实际查看范围时隐藏冗余切换框并直接显示真实范围。
4. **2026-07-25** 个人看板：每笔已认领款可在摘要下展开查看对应的部门、中心/小组、项目、认领人和金额；默认收起，避免分摊较多时撑高页面。
5. **2026-07-15** 项目管理：财务后台支持新增部门、在指定部门下新增中心/小组，再继续新增项目；新增目录持久化到 SQLite，即时同步到认领、分摊和成员设置的级联选项。
6. **2026-07-10** 分摊认领：允许退款项目填写负数，按正负明细净额校验到款金额；0 元、净合计不为正数或超出剩余到款仍会拦截，个人看板和纯文本导出同步保留负数明细。

## 当前待办（按优先级）

1. **【最高】正式部署**：按 `DEPLOY.md` 部署到 Zeabur 或腾讯云轻量，替换临时隧道；配持久卷、环境变量，回填飞书回调地址并在飞书重新发布。
2. **部署后收尾**：飞书应用可见范围设为全员/目标同事；重置一次飞书 App Secret（曾明文出现过）；确认正式环境数据库已备份且为空库启动。
3. **转交 Codex** 继续维护。

## 已知问题

- 临时隧道（trycloudflare）地址会变、跨天失效，仅开发期可用（正式部署后消失）。
- 飞书部门未自动从组织架构读取，靠用户首次自选 + 管理员后台调整（原因见 `DECISIONS.md`，是有意取舍）。
- 本地 `claim_pool.db` 不随 git 同步，可能含本机验证或重新上传的数据；正式服务器如果挂了持久化 SQLite，需要在服务器上单独备份/清空。
- 驳回通知依赖接收人在飞书应用可见范围内，范围外发送失败（容错不报错，但人收不到）。
- 入池群通知依赖 `FEISHU_NOTIFY_CHAT_ID`、机器人入群、`im:message:send_as_bot` 权限和应用重新发布；未配置或发送失败不阻断确认入池。
- 无银行流水号的表格行不自动去重；若整份文件重复上传，会再次入池，导入前需确认文件未处理过。
- 单文件 `app.py`（约 4800 行、HTML 内联），已有少量回归测试，复杂页面仍主要靠手动验证。
- SQLite 单文件，适合当前小规模（内部工具、几十人）；并发写多/数据量大需迁 PostgreSQL。数据库不在 git，需单独备份。
